# Project
GCP_PROJECT_ID=mini-ecd
VERTEX_LOCATION=europe-west1
VERTEX_MODEL=gemini-1.5-pro

# One of the following (prefer base64 for CI)
# Base64-encoded JSON string of the service account key
GCP_SERVICE_ACCOUNT_JSON_BASE64=

# Raw JSON string (avoid newlines issues, use base64 instead if possible)
# GCP_SERVICE_ACCOUNT_JSON='{"type":"service_account","project_id":"mini-ecd",...}'

# Alternative: file path, if you really must use a file
# GOOGLE_APPLICATION_CREDENTIALS=./service-account.json

